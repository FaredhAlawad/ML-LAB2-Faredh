# ML-LAB2-Faredh
 define the machine learning problem: 
 This dataset includes the Student Performance dataset includes academic, demographic, and social features about students (e.g., age, study time, absences, and family-related factors). It is a tabular dataset with both numerical and categorical variables.
The dataset is a supervised regression problem where the goal is to predict a student’s final grade using multiple input features. It is regression because the output is numeric value.
The target variable is G3 (Final Grade).
